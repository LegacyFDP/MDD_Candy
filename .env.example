# Node environment
NODE_ENV=production

# The app database is a single SQLite file (see db/init-sqlite.cjs); no
# connection string or credentials are needed. The supported server process
# uses server/fete_store.db, configured by server/.env.
