# Node environment
NODE_ENV=production

# Required by the server runtime.
# Path to the live SQLite database file.
DB_PATH=./MDD_Candy.db

# Directory where backup files are created and listed/deleted by admins.
DB_BACKUP_DIR=./backups
