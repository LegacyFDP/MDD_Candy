timmi@ubuntu:/opt/fete_store$ docker compose logs backend
fete-store-backend  | Initializing SQLite database at /data/fete_store.db...
fete-store-backend  | ✓ Schema created
fete-store-backend  | ✓ Seed data loaded
fete-store-backend  | ✅ Database initialized successfully!
fete-store-backend  |
fete-store-backend  | > fete-store-server@1.0.0 start
fete-store-backend  | > tsx src/index.ts
fete-store-backend  |
fete-store-backend  | Connected to SQLite database at /data/fete_store.db
fete-store-backend  | Fete Store Manager API listening on http://0.0.0.0:8080
fete-store-backend  | Loaded 25 backend functions

server {
    listen 80;

    # This routes requests from the browser to your backend container
    location /api/ {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}